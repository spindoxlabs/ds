# EDC filesystem vault for the grid operator (`DID-15`).
# Keys here are referenced by alias from `grid-operator.properties`.

# STS client secret — must match this participant's `sts_client_secret` in its
# own identity-registry instance (`IR_GRID_OPERATOR_STS_SECRET`).
sts-grid-operator-client-secret=insecure-dev-secret

# EDR signing key, used by this data plane for Endpoint Data Reference tokens.
# **Not** the DID/identity key — those are the identity-registry's and never
# appear in a file the EDC can read. Distinct from the other participants': one
# shared key would let either data plane mint tokens the other accepts.
participant-private-key={"kty":"EC","crv":"P-256","x":"VRSMmFb0n5vlOQsElSmUsdbRfS-7d3_lV2UfFKcsYfA","y":"VA7sRTc3fV4ZDhOpr8yRdpqmvecu-ab-CfG0w8fq8tQ","d":"8p0Z5agzzbtILFkCBR-ouYKXxijj23fIa17FqauvCic","kid":"edr-grid-operator-key-1","use":"sig"}
