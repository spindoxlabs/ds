version: "3"

vars:
  SRC: src/provenance

tasks:
  setup:
    desc: Install Python dependencies (idempotent)
    cmds:
      - uv sync --extra dev
    sources: [pyproject.toml]
    generates: [.venv/bin/uvicorn]

  run:
    desc: Run dev server with hot-reload on :30000
    deps: [setup]
    cmds:
      - uv run uvicorn provenance.main:app --reload --port 30000

  debug:
    desc: Run dev server with debugpy on :30900, waiting for attach
    deps: [setup]
    cmds:
      - uv run python -m debugpy --listen 0.0.0.0:30900 --wait-for-client -m uvicorn provenance.main:app --reload --port 30000

  release:
    desc: (noop — managed by CI/CD)
    cmds:
      - echo "release not configured"

  db:migrate:
    desc: Apply pending migrations (requires postgres)
    cmds:
      - uv run alembic upgrade head

  db:revision:
    desc: "Generate a new migration — pass MESSAGE=my_description"
    cmds:
      - uv run alembic revision --autogenerate -m "{{.MESSAGE | default "migration"}}"

  test:
    cmds:
      - uv run pytest tests/ -v

  lint:
    cmds:
      - uv run ruff check {{.SRC}} tests/

  format:
    cmds:
      - uv run ruff format {{.SRC}} tests/

  type-check:
    cmds:
      - uv run mypy {{.SRC}}
