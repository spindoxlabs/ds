[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ds-provenance"
version = "0.1.0"
description = "W3C PROV-O compatible provenance and audit service"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
    "pydantic>=2.7",
    "pydantic-settings>=2.4",
    "sqlalchemy[asyncio]>=2.0",
    "aiosqlite>=0.20",
    "asyncpg>=0.29",
    "alembic>=1.13",
    "python-ulid>=2.0",
    "pyjwt[crypto]>=2.9",
    "ds-auth",
    "ds-obs[tracing]",
]

[project.optional-dependencies]
dev = ["pytest>=8", "pytest-asyncio>=0.24", "ruff>=0.6", "mypy>=1.11"]

[tool.uv.sources]
ds-auth = { path = "../../libs/ds-auth", editable = true }
ds-obs = { path = "../../libs/ds-obs", editable = true }

[tool.hatch.build.targets.wheel]
packages = ["src/provenance"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
# `tests/integration` needs a real Postgres, so it is never collected by
# `task test` — the unit suite stays fast and dependency-free.
# `task test:integration` names the directory explicitly.
norecursedirs = ["tests/integration"]
markers = ["integration: needs a real PostgreSQL on 172.17.0.1:35432"]

[dependency-groups]
dev = [
    "debugpy>=1.8.21",
]
