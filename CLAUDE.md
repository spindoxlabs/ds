Read `AGENTS.md`

Ensure to load, if they exists, also `services/*/AGENTS.md` and `libs/*/AGENTS.md` based on the context of work. 

If an `AGENTS.md` does not exists yet, suggest to create it. If it exists and need to be updated after your changes, suggest an update.