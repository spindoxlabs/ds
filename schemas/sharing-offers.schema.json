{
  "$defs": {
    "OfferCoverage": {
      "description": "How far back and how far forward the sharing reaches.",
      "properties": {
        "prospective": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Prospective"
        },
        "retrospective": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Retrospective"
        }
      },
      "title": "OfferCoverage",
      "type": "object"
    },
    "OfferRecipients": {
      "anyOf": [
        {
          "required": [
            "recipient"
          ]
        },
        {
          "required": [
            "controller"
          ]
        }
      ],
      "description": "Who may receive the data, and in what capacity.\n\n``recipient`` is **the party the data goes to**: the DSP consumer EDC puts in\n``counterPartyId``, ODRL 2.2's ``odrl:recipient`` (*\"the party receiving the\nresult/outcome of exercising the action of the Rule\"*), and the DSSC data\nrecipient.  It is an owner alias, resolved to a DID through the owner\nregistry wherever it is enforced.\n\n**It was called ``controller`` and meant three things at once** — the\nrecipient, the subject's home organisation, and the GDPR Art. 4(7)\ncontroller.  Checked against a real four-hop chain, only the first reading\nheld in every offer, so that is the one the field keeps\n(plan ``every-personal-dataset-asks-for-a-local-consent``, 2026-09-17).  The\nsubject's home organisation is now the *collecting* organisation, established\nfrom the caller's token at the consent write\n(``a-collector-registers-consent-at-the-holder``); the GDPR controller, where\na text needs to name one, is described in the offer's consent text and is not\nused for enforcement.\n\n``controller`` / ``controller_role`` are still **accepted on input** and\nfolded in by :meth:`_accept_legacy_spelling`, so no governance file has to\nchange for this model to.\n\nIndependent recipients are never bundled — each one is its own offer, because\nconsent under Art. 4(11) is consent to a *specific* recipient's processing.\n``recipient_role`` names which function of that entity is acting: a grid\noperator's grid-operations and metering functions are different controllers\nunder unbundling rules, same legal entity (``D-11``).\n\n**``recipient_role`` is not a participant role.**  A participant's roles are\nDSP capacities — the identity-registry pins them to ``{provider, consumer}``\n(``schemas/requests.py``) — and ``metering`` is neither.  The vocabulary this\nis checked against is the one declared in ``recipient_roles`` beside the\noffers; see :class:`SharingOfferCatalogue`.",
      "properties": {
        "controller": {
          "deprecated": true,
          "description": "Deprecated spelling of `recipient` — still read, still warned about.",
          "type": "string"
        },
        "controller_role": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "deprecated": true,
          "description": "Deprecated spelling of `recipient_role`."
        },
        "processors": {
          "$ref": "#/$defs/ProcessorCategory"
        },
        "recipient": {
          "title": "Recipient",
          "type": "string"
        },
        "recipient_role": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Recipient Role"
        }
      },
      "required": [
        "processors"
      ],
      "title": "OfferRecipients",
      "type": "object"
    },
    "ProcessorCategory": {
      "description": "A *category* of recipient, not a list of names.\n\nProcessors act on the controller's instructions under a DPA (GDPR Art. 28),\nso a new one joining the category is disclosed rather than re-consented.\n``admitted_by`` is what makes the category checkable instead of a promise:\neach entry is a constraint the platform can evaluate at negotiation time.",
      "properties": {
        "admitted_by": {
          "items": {
            "additionalProperties": true,
            "type": "object"
          },
          "title": "Admitted By",
          "type": "array"
        },
        "category": {
          "title": "Category",
          "type": "string"
        }
      },
      "required": [
        "category"
      ],
      "title": "ProcessorCategory",
      "type": "object"
    },
    "sharingOffer": {
      "additionalProperties": false,
      "description": "A purpose-scoped bundle a person is asked to consent to.",
      "properties": {
        "consent_text_version": {
          "default": "1.0",
          "title": "Consent Text Version",
          "type": "string"
        },
        "coverage": {
          "$ref": "#/$defs/OfferCoverage"
        },
        "id": {
          "title": "Id",
          "type": "string"
        },
        "legal_basis": {
          "title": "Legal Basis",
          "type": "string"
        },
        "measures": {
          "items": {
            "type": "string"
          },
          "title": "Measures",
          "type": "array"
        },
        "purpose": {
          "title": "Purpose",
          "type": "string"
        },
        "recipients": {
          "$ref": "#/$defs/OfferRecipients"
        },
        "requires_offers": {
          "items": {
            "type": "string"
          },
          "title": "Requires Offers",
          "type": "array"
        },
        "resolution": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Resolution"
        },
        "retention": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Retention"
        },
        "revocable": {
          "default": true,
          "title": "Revocable",
          "type": "boolean"
        },
        "subject_scope": {
          "default": "own_data",
          "title": "Subject Scope",
          "type": "string"
        }
      },
      "required": [
        "id",
        "purpose",
        "legal_basis",
        "recipients"
      ],
      "title": "Sharing offer",
      "type": "object"
    }
  },
  "$id": "https://spindoxlabs.github.io/ds/schemas/sharing-offers.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Sharing offers declared alongside a governance.yaml. Generated from the SharingOffer model in ds-governance — do not edit by hand.",
  "properties": {
    "controller_roles": {
      "additionalProperties": {
        "items": {
          "type": "string"
        },
        "type": "array"
      },
      "deprecated": true,
      "description": "Deprecated spelling of recipient_roles — still read, still warned about. Rename it.",
      "type": "object"
    },
    "recipient_roles": {
      "additionalProperties": {
        "items": {
          "type": "string"
        },
        "type": "array"
      },
      "description": "recipient alias -> the controller functions that entity is unbundled into. The vocabulary an offer's recipient_role is checked against; not the identity-registry's participant roles.",
      "type": "object"
    },
    "sharing_offers": {
      "items": {
        "$ref": "#/$defs/sharingOffer"
      },
      "type": "array"
    }
  },
  "required": [
    "sharing_offers"
  ],
  "title": "ds sharing offers",
  "type": "object"
}
