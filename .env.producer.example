# Producer participant env. Copy to .env.producer for local development.
# All defaults work out of the box with shared infra running.

# ── Shared secrets ─────────────────────────────────────────────────────────────
EDC_API_KEY=insecure-dev-key

# ── Dataset API ────────────────────────────────────────────────────────────────
# Point to an external dataset API with realistic data (leave empty for mock):
# DATASET_API_EXTERNAL_QUERY_URL=http://your-real-api:8080/query
